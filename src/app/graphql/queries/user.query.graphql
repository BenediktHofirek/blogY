query UserQuery($id: String!) {
  user(id: $id) {
    id
    username
    email
    description
    photoUrl
    createdAt
    updatedAt
    blogList {
      id
      name
    }
    articleList {
      id
      name
    }
  }
}