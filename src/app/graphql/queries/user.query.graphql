query UserQuery($userId: ID!) {
  user(userId: $userId) {
    id
    username
    email
    firstName
    lastName
    description
    image
    createdAt
    updatedAt
    blogList {
      id
      name
    }
    articleList {
      id
      name
    }
  }
}