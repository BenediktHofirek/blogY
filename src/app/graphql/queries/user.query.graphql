query UserQuery($userId: ID!) {
  user(userId: $userId) {
    id
    username
    email
    firstName
    lastName
    description
    photoUrl
    createdAt
    updatedAt
    blogList {
      id
      name
    }
    articleList {
      id
      name
    }
  }
}